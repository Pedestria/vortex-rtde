/**Creates a star Package
 * 
 * @param {string} resolvedPath A resolved Path to the vortex panel config!
 */

declare function createStarPackage(resolvedPath:string):Promise<void>

export = createStarPackage
